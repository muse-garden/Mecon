# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，版本号遵循
Semantic Versioning。未发布的用户可见变化先记录在 `Unreleased`，发布时再归档到版本号。

## [Unreleased]

### Changed

- 动态规划求解器优化，实测性能已无问题，默认开启。

### Fixed

- 部分规则因id命名原因，并未接入自由练习，已修复。

## [0.1.0] - 2026-08-10

### Added

- 自由练习新增共享时间线 raw scene/手势控制器、统一双层工具栏描述符，以及 JVM/JS raw-input、Web/桌面截图和浏览器导出回读门禁。
- 增加 GPLv3-or-later 许可证、贡献指南、PR 模板和开发文档。
- 增加行为准则、安全问题报告说明与 issue 模板。
- 增加 renderer 增量布局/渲染的职责文档和精简后的性能路线。

### Changed

- 调整自由练习和声时间轴的惯用进行标记：桌面与 Web 共用更清晰的大号标签、随标题宽度伸缩并遮挡后方拍点的深色底罩，以及更短的两端竖线。
- 渲染和增量更新文档改为维护当前结论、TODO 与关键注意事项，不再堆叠逐次进度日志。
- 回归测试改用合成乐谱，移除对未授权外部乐谱文件的依赖。

### Fixed

- 修正自由练习和声时间轴拖动到谱面末尾之外的行为：轴末尾的小节线锚点会把整拍时值压进几个像素，原先据此外推，导致指针每移动一点就跳过多个小节，Ctrl 拖动也会压扁最右侧的和弦；改为按请求的比例间距（不低于全轴平均密度）外推，并允许内容宽度超出记谱表面。
- 修正桌面自由练习和声时间轴松开鼠标后闪回拖动前状态的问题：workspace 由副作用回写，提交后的下一帧仍是旧值；预览现在保留到被提交结果取代，仅在会话拒绝该编辑时立即丢弃。
- 修正高分屏下桌面和声时间轴和弦框变矮的问题：scene 改为按与浏览器 CSS 像素等价的密度无关单位排版，由 Compose 统一还原显示缩放。
- 修正自动写作运行期间时间轴手势的基准：待写作的编辑尚未提交、只存在于后台请求中，而它正是各视图显示的内容；此前手势按最后提交的 workspace 解析，导致预览被整体拒绝（拖动看不到预览）、下一次提交还会把待写作的编辑悄悄回退。现统一以可见 workspace 为编辑基准并顶替待处理请求。
- 修正自由练习和弦拖动预览"看不见"的问题：解析出的时间轴末尾会把整小节音乐时间压进几个像素，落在该区段内的和弦（谱面短于时间轴时新加的和弦即在此，撤销后尤其明显）拖动一个网格步只移动约 2px，预览等同于不动，其逆映射也让每个指针像素跨越数个小节。`TimeScale` 现在丢弃密度低于请求间距四分之一的退化锚点区段，改用与轴外一致的比例外推；共享代码，桌面与 Web 同时生效。
- 修正桌面自由练习面板渲染的 workspace 与会话编辑基准不一致的问题：面板此前渲染仅含已提交状态的 workspace，且只在没有后台任务时才同步，而写作之后紧接着的优化任务让这一条件几乎不成立，撤销期间时间轴会继续画出会话已丢弃的和弦。改为渲染会话可见 workspace（`practiceWorkspace`，与 `editBase` 同源）。
- 修正桌面和声时间轴拖动预览在 workspace 刷新后消失的问题：controller 只在量化编辑变化时下发预览，指针停留在同一网格内时不再下发，而面板把投影钉在生成它的 base 上，其间任何 workspace 重新发布都会让预览在该次拖动剩余过程中消失；改为保存预览编辑并在 base 变化时重新投影。
- 修正高分屏下桌面和声时间轴文字整体偏上的问题：文字基线补偿是按设备像素标定的常量，随显示缩放放大后在 150% 下变成 4.5px，把标签整体往上拉；改为不随缩放增长（100% 渲染逐字节不变）。
- 修正自由练习五线谱末尾小节不随和弦时间轴收缩的问题：`ensureTimelineMeasures` 此前只增不减，向左拖动或删除和弦后空小节会堆在谱尾；现同步裁掉时间轴不再覆盖的小节及其中的孤立音符。
- 修正文档中指向不存在 `docs/development.md` 的入口，补齐构建与测试说明。
- 恢复自由练习和声时间轴的悬停反馈：指针移到和弦、手柄、共享边界、调性线、惯用进行和插入按钮上时重新显示对应鼠标指针与高亮，桌面与 Web 由共享 scene 统一给出。
